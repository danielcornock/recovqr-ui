import { FormInputErrorHandlerDirective } from './form-input-error-handler.directive';

describe('FormInputErrorHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new FormInputErrorHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
